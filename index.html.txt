<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>GoalPro Store — Cinematic Football Shop</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <!-- NAVBAR -->
  <header class="site-header" id="top">
    <div class="nav-wrap">
      <div class="brand">
        <div class="logo">GP<span>O</span></div>
        <div class="brand-text">
          <div class="name">GoalPro Store</div>
          <div class="tag">Football & Accessories</div>
        </div>
      </div>

      <nav class="nav" aria-label="Main navigation">
        <a href="#home">Home</a>
        <a href="#shop">Shop</a>
        <a href="#accessories">Accessories</a>
        <a href="#about">About</a>
        <a href="#testimonials">Testimonials</a>
        <a href="#contact">Contact</a>
      </nav>

      <div class="actions">
        <button class="ghost" id="searchBtn" aria-label="Search">🔍</button>
        <button class="ghost" aria-label="Wishlist">♡</button>
        <button class="primary" id="openCart" aria-label="Open cart">Cart <span id="cartCount">(0)</span></button>
      </div>
    </div>

    <!-- HERO -->
    <section class="hero reveal" id="home" aria-label="Hero">
      <div class="hero-inner">
        <div class="hero-left">
          <h1 class="reveal-up">Rule the Game</h1>
          <p class="reveal-up delay-1">Premium match balls, pro boots and high-performance accessories — designed for champions. Fast delivery across Pakistan.</p>
          <div class="hero-ctas reveal-up delay-2">
            <a href="#shop" class="btn btn-primary">Shop Now</a>
            <a href="#accessories" class="btn btn-outline">View Accessories</a>
          </div>

          <ul class="hero-feats reveal-up delay-3">
            <li>Official size 5 match balls</li>
            <li>Pro-grade boots & gear</li>
            <li>Easy payments • 7-day returns</li>
          </ul>
        </div>

        <div class="hero-right" aria-hidden="true">
          <div class="stadium-glow"></div>
          <div class="ball-3d parallax"></div>
        </div>
      </div>
    </section>
  </header>

  <main>
    <!-- SHOP / PRODUCTS -->
    <section class="section" id="shop" aria-labelledby="shop-heading">
      <div class="container">
        <h2 id="shop-heading" class="reveal">Featured Products</h2>
        <p class="lead reveal delay-1">Top sellers hand-picked for performance and durability.</p>

        <div class="grid products-grid" role="list">
          <!-- Product Card 1 -->
          <article class="card reveal-up" role="listitem" data-price="4499">
            <div class="thumb" style="background-image:url('https://images.unsplash.com/photo-1600707482851-4a7a4a2c8f86?auto=format&fit=crop&w=800&q=60')"></div>
            <div class="card-body">
              <h3>MatchPro Elite</h3>
              <p class="muted">Official size 5 • Machine-stitched</p>
              <div class="card-row">
                <div class="price">PKR 4,499</div>
                <button class="small-btn add-to-cart">Add</button>
              </div>
            </div>
          </article>

          <!-- Product Card 2 -->
          <article class="card reveal-up delay-1" role="listitem" data-price="1299">
            <div class="thumb" style="background-image:url('https://images.unsplash.com/photo-1521572267360-ee0c2909d5d1?auto=format&fit=crop&w=800&q=60')"></div>
            <div class="card-body">
              <h3>GripPro Gloves</h3>
              <p class="muted">Breathable • Anti-slip</p>
              <div class="card-row">
                <div class="price">PKR 1,299</div>
                <button class="small-btn add-to-cart">Add</button>
              </div>
            </div>
          </article>

          <!-- Product Card 3 -->
          <article class="card reveal-up delay-2" role="listitem" data-price="8999">
            <div class="thumb" style="background-image:url('https://images.unsplash.com/photo-1542291026-7eec264c27ff?auto=format&fit=crop&w=800&q=60')"></div>
            <div class="card-body">
              <h3>Stadium Boots X</h3>
              <p class="muted">Firm ground • Lightweight</p>
              <div class="card-row">
                <div class="price">PKR 8,999</div>
                <button class="small-btn add-to-cart">Add</button>
              </div>
            </div>
          </article>

          <!-- Product Card 4 -->
          <article class="card reveal-up delay-3" role="listitem" data-price="699">
            <div class="thumb" style="background-image:url('https://images.unsplash.com/photo-1520975681916-9efb6bd6f0a6?auto=format&fit=crop&w=800&q=60')"></div>
            <div class="card-body">
              <h3>Training Cones (12pcs)</h3>
              <p class="muted">Flexible • Pack</p>
              <div class="card-row">
                <div class="price">PKR 699</div>
                <button class="small-btn add-to-cart">Add</button>
              </div>
            </div>
          </article>
        </div>
      </div>
    </section>

    <!-- ACCESSORIES -->
    <section class="section dark-section" id="accessories" aria-labelledby="acc-heading">
      <div class="container">
        <h2 id="acc-heading" class="reveal">Accessories</h2>
        <p class="lead reveal delay-1">Everything to support your training — grips, cones, pumps and more.</p>

        <div class="features-row">
          <div class="feature reveal-up delay-1">
            <div class="icon">⚙️</div>
            <h4>Durable Pumps</h4>
            <p class="muted">Compact pumps for quick inflation anywhere.</p>
          </div>
          <div class="feature reveal-up delay-2">
            <div class="icon">🧤</div>
            <h4>Keeper Gloves</h4>
            <p class="muted">Grip that holds in wet conditions.</p>
          </div>
          <div class="feature reveal-up delay-3">
            <div class="icon">🎒</div>
            <h4>Training Bags</h4>
            <p class="muted">Carry your kit with pro-level comfort.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- ABOUT -->
    <section class="section" id="about" aria-labelledby="about-heading">
      <div class="container about-grid">
        <div class="about-media reveal" aria-hidden="true" style="background-image:url('https://images.unsplash.com/photo-1517649763962-0c623066013b?auto=format&fit=crop&w=1200&q=60')"></div>
        <div class="about-text reveal-up delay-1">
          <h2 id="about-heading">About GoalPro</h2>
          <p>GoalPro started with one mission: to bring pro-level football gear to local players. We test every product on the pitch and guarantee satisfaction.</p>
          <ul class="check-list">
            <li>Pro-tested equipment</li>
            <li>Nationwide shipping</li>
            <li>Secure payments & easy returns</li>
          </ul>
          <a href="#contact" class="btn btn-primary">Get in Touch</a>
        </div>
      </div>
    </section>

    <!-- TESTIMONIALS -->
    <section class="section dark-section" id="testimonials" aria-labelledby="test-heading">
      <div class="container">
        <h2 id="test-heading" class="reveal">What Players Say</h2>
        <div class="testimonials">
          <div class="testi reveal-up delay-1">
            <div class="avatar">AS</div>
            <blockquote>“The MatchPro ball felt perfect in matches. Great flight and durability.”</blockquote>
            <div class="who">— Ali Shah, Forward</div>
          </div>

          <div class="testi reveal-up delay-2">
            <div class="avatar">MR</div>
            <blockquote>“Fast delivery and the boots are comfortable for 90 minutes.”</blockquote>
            <div class="who">— Mariam R., Midfielder</div>
          </div>
        </div>
      </div>
    </section>

    <!-- CONTACT -->
    <section class="section" id="contact" aria-labelledby="contact-heading">
      <div class="container">
        <h2 id="contact-heading" class="reveal">Contact / Order Inquiry</h2>
        <p class="lead reveal delay-1">Drop your details — we'll contact you within 24 hours.</p>

        <form id="contactForm" class="contact-form reveal-up delay-2" onsubmit="return false" aria-label="Contact form">
          <div class="form-row">
            <input id="name" placeholder="Full name" required>
            <input id="phone" placeholder="Phone (e.g. 03xx-xxxxxxx)" required>
          </div>
          <input id="product" placeholder="Product (optional)">
          <textarea id="message" rows="5" placeholder="Your message"></textarea>
          <div class="form-actions">
            <button class="btn btn-primary" id="sendBtn">Send Inquiry</button>
            <button class="btn btn-outline" type="reset">Reset</button>
          </div>
          <div class="hint">We respect your privacy. This is demo frontend only.</div>
        </form>
      </div>
    </section>
  </main>

  <!-- CART MODAL -->
  <div class="overlay" id="cartOverlay" aria-hidden="true">
    <div class="cart-panel" role="dialog" aria-modal="true" aria-label="Cart">
      <div class="cart-head">
        <h3>Your Cart</h3>
        <button id="closeCart" class="close" aria-label="Close cart">✕</button>
      </div>
      <div id="cartItems" class="cart-items">
        <div class="empty">Your cart is empty.</div>
      </div>
      <div class="cart-foot">
        <div class="total-row"><span>Total</span><span id="cartTotal">PKR 0</span></div>
        <div class="cart-actions">
          <button class="btn btn-primary" onclick="alert('Checkout demo — no backend')">Checkout</button>
          <button class="btn btn-outline" id="clearCart">Clear</button>
        </div>
      </div>
    </div>
  </div>

  <!-- FOOTER -->
  <footer class="site-footer">
    <div class="container footer-grid">
      <div>
        <div class="logo-sm">GP<span>O</span></div>
        <p class="muted">GoalPro Store — Premium Football & Accessories</p>
        <div class="copy">© 2025 GoalPro Store</div>
      </div>
      <div>
        <h4>Shop</h4>
        <a href="#shop">Footballs</a><br>
        <a href="#accessories">Accessories</a><br>
        <a href="#contact">Contact</a>
      </div>
      <div>
        <h4>Support</h4>
        <a href="#">Shipping</a><br>
        <a href="#">Returns</a><br>
        <a href="#">Payments</a>
      </div>
      <div>
        <h4>Follow</h4>
        <div class="socials">
          <span class="social">IG</span>
          <span class="social">FB</span>
          <span class="social">TW</span>
        </div>
      </div>
    </div>
  </footer>

<script>
/* --- Simple front-end cart + form behaviour (no backend) --- */
const cart = [];
const cartCount = document.getElementById('cartCount');
const cartOverlay = document.getElementById('cartOverlay');
const cartItems = document.getElementById('cartItems');
const cartTotalEl = document.getElementById('cartTotal');

function formatPKR(n){ return 'PKR ' + n.toLocaleString(); }

function updateCartUI(){
  cartCount.textContent = '(' + cart.length + ')';
  cartItems.innerHTML = '';
  if(cart.length === 0){
    cartItems.innerHTML = '<div class="empty">Your cart is empty.</div>';
    cartTotalEl.textContent = 'PKR 0';
    return;
  }
  let total = 0;
  cart.forEach((item, idx) => {
    total += item.price;
    const div = document.createElement('div');
    div.className = 'cart-item';
    div.innerHTML = `
      <div class="ci-left">
        <div class="ci-thumb" style="background-image:url('${item.img}')"></div>
        <div>
          <div class="ci-title">${item.title}</div>
          <div class="muted">Qty: 1</div>
        </div>
      </div>
      <div class="ci-right">
        <div class="ci-price">${formatPKR(item.price)}</div>
        <button class="small-btn remove" data-index="${idx}">Remove</button>
      </div>
    `;
    cartItems.appendChild(div);
  });
  cartTotalEl.textContent = formatPKR(total);
  // remove handlers
  document.querySelectorAll('.remove').forEach(btn=>{
    btn.addEventListener('click', e=>{
      const i = Number(e.currentTarget.dataset.index);
      cart.splice(i,1);
      updateCartUI();
    });
  });
}

document.querySelectorAll('.add-to-cart').forEach((btn, i)=>{
  btn.addEventListener('click', (e)=>{
    const card = e.currentTarget.closest('.card');
    const title = card.querySelector('h3').innerText;
    const price = Number(card.dataset.price);
    const imgStyle = card.querySelector('.thumb').style.backgroundImage;
    const img = imgStyle.slice(5,-2);
    cart.push({title, price, img});
    updateCartUI();
    // show mini confirmation
    btn.textContent = '✓ Added';
    setTimeout(()=> btn.textContent = 'Add', 1200);
  });
});

document.getElementById('openCart').addEventListener('click', ()=>{
  cartOverlay.classList.add('show');
  cartOverlay.setAttribute('aria-hidden','false');
});

document.getElementById('closeCart').addEventListener('click', ()=>{
  cartOverlay.classList.remove('show');
  cartOverlay.setAttribute('aria-hidden','true');
});

document.getElementById('clearCart').addEventListener('click', ()=>{
  cart.length = 0;
  updateCartUI();
});

cartOverlay.addEventListener('click', e=>{ if(e.target === cartOverlay){ cartOverlay.classList.remove('show'); cartOverlay.setAttribute('aria-hidden','true'); } });

/* Contact form */
document.getElementById('sendBtn').addEventListener('click', ()=>{
  const name = document.getElementById('name').value.trim();
  const phone = document.getElementById('phone').value.trim();
  if(!name || !phone){ alert('Please enter your name and phone'); return; }
  alert('Thanks ' + name + '! We received your inquiry. (Demo front-end)');
  document.getElementById('contactForm').reset();
});

/* Smooth scroll for nav */
document.querySelectorAll('.nav a').forEach(a=>{
  a.addEventListener('click', e=>{
    e.preventDefault();
    const href = a.getAttribute('href');
    if(href.startsWith('#')){
      document.querySelector(href).scrollIntoView({behavior:'smooth', block:'start'});
    }
  });
});

/* --------------------------
   Scroll reveal (IntersectionObserver)
   -------------------------- */
const revealOptions = {
  threshold: 0.15,
  rootMargin: '0px 0px -10% 0px'
};
const revealObserver = new IntersectionObserver((entries, obs) => {
  entries.forEach(entry => {
    if(entry.isIntersecting){
      entry.target.classList.add('in-view');
      // once visible, unobserve to improve perf
      obs.unobserve(entry.target);
    }
  });
}, revealOptions);

// select reveals
document.querySelectorAll('.reveal, .reveal-up, .reveal-left, .reveal-right').forEach(el => {
  revealObserver.observe(el);
});

/* Parallax subtle on mousemove for hero ball */
const parallaxEl = document.querySelector('.parallax');
if(parallaxEl){
  document.querySelector('.hero').addEventListener('mousemove', (e) => {
    const rect = parallaxEl.getBoundingClientRect();
    const cx = window.innerWidth/2;
    const cy = rect.top + rect.height/2;
    const dx = (e.clientX - cx) * 0.008;
    const dy = (e.clientY - cy) * 0.01;
    parallaxEl.style.transform = `translate3d(${dx}px, ${dy}px, 0) rotate(-8deg)`;
  });
  document.querySelector('.hero').addEventListener('mouseleave', ()=> {
    parallaxEl.style.transform = 'translate3d(0,0,0) rotate(-8deg)';
  });
}

/* Initial UI */
updateCartUI();
</script>
</body>
</html>
